2.PHP 发送定时公告到Nodejs-------->  Nodejs 接收到存入mongodb, 再加入计划列表 返回 
3.NodeJS检测到定时到了，就向PHP 发消息 
	1),删除该事情的计划任务
	2),删除mongodb数据库里的数据,写入日志
	3),模拟userbak 用户登陆
    4),传入要发送的公告ID，PHP拿出mysql 数据，进行处理，调用游戏服务器接口	
4.NodeJS 如果挂掉了，重启后，会先去检索mongodb里面的公告，加入到计划任务列表
5.注意:一个服务器，只能定一次时,这次定时会把上一次的计划任务给踢除，在存入mongodb时会检查是否已存在，加入计划列表时，也会检查并踢除	
